{
  "name": "node-cf-demo",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node dist/server.js",
    "build": "esbuild server.js --bundle --platform=node --outfile=dist/server.js"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "querystring": "^0.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
